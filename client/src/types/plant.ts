type Plant = {
  id: string;
  name: string;
  species: string;
  lastWatered: Date;
  nextWatering: Date;
  soilType: string;
  sunlightRequirements: string;
  wateringFrequency: number;
  notes: string;
};

export type { Plant };
